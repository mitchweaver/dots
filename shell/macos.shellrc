# GNU coreutils replacing MacOS outdated coreutils
if [ "$(uname)" = Darwin ] ; then
    if [ -d /opt/homebrew/opt/coreutils ] ; then
        export PATH="/opt/homebrew/opt/coreutils/libexec/gnubin:$PATH"
    fi
fi

