# programs
alt Return
    kitty --single-instance
alt + w
    brws
# alt + r
#     kitty --single-instance ranger

super + l
    xautolock -locknow -locker 'i3lock-fancy-rapid 20 pixel'

Print
    scrap -n
control + Print
    scrap

#alt + e
    # pcmanfm
# super + e
    # pcmanfm

# dmenus
alt + p
    dmenu_run -p 'Run:'
    # menu run -p 'Run:'
alt + o
    dedit
# alt + y
    # dmoji

# brightness
XF86MonBrightnessUp
    bright i
XF86MonBrightnessDown
    bright d

# redshift control
# alt + equal
#     sctc i
# alt + minus
#     sctc d

# volume
alt + apostrophe
    vol i
alt + semicolon
    vol d
XF86VolumeUp
    vol i
XF86VolumeDown
    vol d

# media / mpv
# alt + slash
#     mmt -t
# alt + period
#     mmt -n
# alt + comma
#     mmt -p
# alt + bracketright
#     mmt -f
# alt + bracketleft
#     mmt -b
